# Sequence-Detector
Implementation of a Finite State Machine (FSM) based sequence detector using Verilog HDL.  The design detects a specific bit pattern in a serial input stream and verifies functionality  through ModelSim simulation and waveform analysis.
